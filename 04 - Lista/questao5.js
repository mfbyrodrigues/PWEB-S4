// Par ou ímpar 

let numero = prompt ("Digite um número:"); // Número digitado pelo usuário

if (numero % 2 == 0) { // Verifica se o número possui resto zero para ser par
    console.log ("Número par!");
}
else { // Caso não seja, ímpar
    console.log ("Número ímpar!");
}